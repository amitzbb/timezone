apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "timezone.fullname" . }}-test-connection"
  labels:
    {{- include "timezone.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "timezone.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
